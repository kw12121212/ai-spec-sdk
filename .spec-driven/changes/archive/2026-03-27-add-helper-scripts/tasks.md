# Tasks: add-helper-scripts

## Implementation

- [x] Create `scripts/clean.sh` — remove dist/ directory
- [x] Create `scripts/build.sh` — clean then build via `bun run build`
- [x] Create `scripts/test.sh` — run tests via `bun test`, pass through args
- [x] Create `scripts/check.sh` — run lint + typecheck + test sequentially

## Testing

- [x] Verify each script runs successfully
- [x] Verify existing `bun run` scripts still work
- [x] Lint passes (`bun run lint`)

## Verification

- [x] Verify implementation matches proposal
