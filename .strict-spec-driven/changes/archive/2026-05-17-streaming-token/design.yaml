schema: strict-spec-driven/change-design/v1
schema_version: 1
artifact_type: change_design
schema_file: templates/strict-spec-driven/schemas/change-design.yaml
change: streaming-token
approach:
  - >-
    Add stream option to session start/resume, emit bridge/stream_chunk notifications, and update
    CLI renderer
decisions:
  stream-transport:
    decision: Use bridge/stream_chunk notifications
    rationale: Standardizes streaming across stdio and HTTP/SSE transports
alternatives: []
risks: []
