schema: strict-spec-driven/task-list/v2
schema_version: 2
artifact_type: task_list
schema_file: templates/strict-spec-driven/schemas/task-list.yaml
change: streaming-token
sections:
  Implementation:
    update-agent-runner:
      text: Update claude-agent-runner.ts to emit per-chunk events when stream is enabled.
      status: complete
    update-bridge-transports:
      text: Introduce bridge/stream_chunk notification for HTTP/SSE and stdio transports.
      status: complete
    update-go-cli:
      text: Update the Go CLI renderer to handle bridge/stream_chunk progressively.
      status: complete
  Testing:
    run-build:
      text: Run build validation.
      status: complete
      command: bun run build
    run-tests:
      text: Run unit tests.
      status: complete
      command: bun test/run.ts
  Verification:
    verify-cli-streaming:
      text: Verify the Go CLI renders text smoothly during a streaming response.
      status: complete
testing_gates:
  validation_task: run-build
  unit_test_task: run-tests
