schema: strict-spec-driven/task-list/v2
schema_version: 2
artifact_type: task_list
schema_file: templates/strict-spec-driven/schemas/task-list.yaml
change: proposal-token-attribution
sections:
  Implementation:
    implementation-1:
      text: >-
        Add `getMessageUsage(sessionId: string, messageId: string): TokenRecord | undefined` to
        `TokenStore` interface and its implementation.
      status: complete
    implementation-2:
      text: >-
        Ensure `claude-agent-runner.ts` passes down `messageId` into `TokenStore.recordUsage` when
        handling query responses.
      status: complete
    implementation-3:
      text: >-
        Expose `token.getMessageUsage` over the JSON-RPC bridge in `bridge.ts` and handle the
        "Message not found" error (-32052) correctly.
      status: complete
    implementation-4:
      text: Add `token.getMessageUsage` to `bridge.capabilities`.
      status: complete
  Testing:
    testing-1:
      text: Run `bun run lint` to ensure TypeScript compilation and typechecks pass.
      status: complete
      command: bun run lint
    testing-2:
      text: >-
        Run `bun test` to execute unit tests against the new message-level attribution logic and
        bridge methods.
      status: complete
      command: bun test
  Verification:
    verification-1:
      text: Verify implementation matches proposal scope
      status: complete
testing_gates:
  validation_task: testing-1
  unit_test_task: testing-2
