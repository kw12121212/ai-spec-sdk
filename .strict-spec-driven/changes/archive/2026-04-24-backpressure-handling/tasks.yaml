schema: strict-spec-driven/task-list/v2
schema_version: 2
artifact_type: task_list
schema_file: templates/strict-spec-driven/schemas/task-list.yaml
change: backpressure-handling
sections:
  Implementation:
    implementation-1:
      text: >-
        Implement transport buffer monitoring in `src/bridge.ts` (e.g., checking WebSocket
        `bufferedAmount`).
      status: complete
    implementation-2:
      text: >-
        Update `src/session-store.ts` and `src/llm-provider/adapters/anthropic.ts` to support
        pausing/delaying the async iterator when backpressure is requested.
      status: complete
    implementation-3:
      text: Implement hard disconnect logic in `src/bridge.ts` if the critical buffer limit is exceeded.
      status: complete
  Testing:
    testing-1:
      text: Run `npm run lint` — validate no syntax errors or unused variables.
      status: complete
      command: npm run lint
    testing-2:
      text: >-
        Run `npm run test` — add unit tests in `test/session.test.ts` to mock a slow consumer and
        verify the async iterator is paused.
      status: complete
      command: npm run test
    testing-3:
      text: >-
        Run `npm run test` — add unit tests in `test/bridge.test.ts` to verify the hard disconnect
        triggers when the critical limit is exceeded.
      status: complete
      command: npm run test
  Verification:
    verification-1:
      text: Verify implementation matches proposal scope
      status: complete
testing_gates:
  validation_task: testing-1
  unit_test_task: testing-2
