schema: strict-spec-driven/change-proposal/v1
schema_version: 1
artifact_type: change_proposal
schema_file: templates/strict-spec-driven/schemas/change-proposal.yaml
change:
  id: distribution-packaging
  status: proposed
summary:
  what:
    - Cross-platform distribution packaging
  why:
    - Simplify the installation process so users do not need Node.js installed to run the CLI.
scope:
  in:
    - Install scripts (install.sh), Homebrew Formula scaffolding, Go CLI bridge resolution.
  out:
    - Full package registry publishing (apt/yum/choco)
unchanged_behavior:
  - Existing TS bridge compile logic
roadmap:
  milestone: 0017-lightweight-cli
  planned_change: distribution-packaging
