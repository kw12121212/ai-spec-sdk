schema: strict-spec-driven/question-list/v1
schema_version: 1
artifact_type: question_list
schema_file: templates/strict-spec-driven/schemas/question-list.yaml
change: external-vault-adapter
open: []
resolved:
  - id: provider-choice
    question: Which external vault providers (e.g. HashiCorp, AWS) should we implement first?
    context: >-
      The milestone mentions these as examples but doesn't dictate which must be
      built right now.
    status: resolved
    answer: >-
      We will implement HashiCorp Vault first.