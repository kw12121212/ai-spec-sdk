schema: strict-spec-driven/delta-spec/v1
schema_version: 1
artifact_type: delta_spec
schema_file: templates/strict-spec-driven/schemas/delta-spec.yaml
target_spec: workflow-loop-control
mapping:
  implementation:
    - src/workflow/loop-controller.ts
  tests:
    - test/workflow/loop-controller.test.ts
operations:
  ADDED:
    loop-recovery:
      strength: MUST
      statement: >-
        The loop controller MUST persist progress at key transitions and recover from the last
        persisted state upon startup.
      scenarios:
        loop-crash-recovery:
          - GIVEN: An autonomous loop crashed during execution
          - WHEN: The system restarts and initializes the loop controller
          - THEN: The loop resumes from the exact state saved at the last transition point
  MODIFIED: {}
  REMOVED: {}
