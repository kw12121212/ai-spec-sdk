schema: strict-spec-driven/roadmap-milestone/v2
schema_version: 2
artifact_type: roadmap_milestone
schema_file: templates/strict-spec-driven/schemas/roadmap-milestone.yaml
milestone:
  0006-v1-stable:
    title: v1 Stable
goal: >-
  Establish the first stable API guarantee by auto-generating SDK types from the bridge contract and
  delivering the JVM reference implementation.
scope:
  in:
    - >-
      Type generation tooling: bridge-contract.yaml → TypeScript and Python type stubs, replacing
      hand-written type files
    - Java CLI REPL reference implementation matching go-cli feature parity
  out:
    - gRPC transport (long-term candidate, not in v1 scope)
    - Auto-update mechanism for published binaries
    - Kotlin/Scala CLI variants
    - Gradle build for java-cli-demo (Maven only)
done_criteria:
  - >-
    TypeScript and Python client types are generated from bridge-contract.yaml; manual type files
    removed
  - Java CLI compiles with Maven 3+ and connects to bridge subprocess via stdio
  - All milestones 01–05 are complete
planned_changes:
  contract-type-generation:
    status: complete
    summary: auto-generate TypeScript and Python types from bridge-contract.yaml
    details: 'Migration placeholder: add technical and design context for this planned change.'
    depends_on: []
  java-cli-demo:
    status: complete
    summary: Java CLI REPL reference implementation
    details: 'Migration placeholder: add technical and design context for this planned change.'
    depends_on: []
dependencies:
  - 03-platform-reach — stable published artifacts needed before java-cli-demo
  - 05-developer-ecosystem — all integration features complete before issuing v1 API guarantee
risks:
  - >-
    Type generation requires bridge-contract.yaml to remain machine-parseable; any informal
    annotations must be migrated first.
  - java-cli-demo is P2 and can slip to a post-v1 release without blocking the stability guarantee.
status: complete
notes: []
